OPENAI_API_KEY=your_openai_api_key_here
CLOSE_API_KEY=your_close_api_key_here

# Django Settings
# Set DEBUG=False for production
DEBUG=False
# Generate a secure secret key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your_secret_key_here
# Comma-separated list of allowed hosts (e.g., "yourdomain.com,www.yourdomain.com")
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# CORS Settings (comma-separated origins, only used when DEBUG=False)
# Example: "https://yourdomain.com,https://www.yourdomain.com"
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# SSL Settings (set to True if using HTTPS with reverse proxy)
SECURE_SSL_REDIRECT=False
